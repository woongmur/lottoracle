# Changelog

## 0.3.0 — 2026-09-03

### 추가 (당첨금)
- **1~5등 당첨 현황** — 동행복권 API 가 주는 등수별 당첨 게임 수·1게임당 당첨금·등수별 총액과
  회차 총 판매금액을 `Draw.prizes` 에 담는다. 회차 탭에서 회차를 누르면 표로 펼쳐지고,
  당첨 확인 결과에도 같은 표가 함께 나온다.
- **실제 당첨금으로 채점** — 등수별 평균치 대신 그 회차의 실제 금액으로 계산한다.
  옛 캐시처럼 상세가 없는 회차는 열 때 그 회차만 받아 채우고(`Engine.ensure_prizes`),
  네트워크가 막혀 있으면 평균치로 계산하고 화면에 그렇게 밝힌다.
- `GET /api/draw?no=` — 회차 하나의 등수별 당첨 현황.

### 변경
- 구매 버튼이 여는 주소를 동행복권 홈(`https://www.dhlottery.co.kr/`)으로 바꿨다.


### 추가
- **당첨 축하 연출** — 채점에서 당첨이 나오면 화면 양쪽 하단에서 컨페티가 터지고, 등수별 배너가 뜬다
  (1등 금색 "축 1등 당첨" · 2등 은색 · 3등 동색 · 4~5등 기본 · 낙첨은 담백하게). 내 번호 탭에서
  추첨 결과가 당첨이어도 같은 연출이 나온다. 외부 라이브러리 없이 canvas 로 그린다.
- **이 번호로 로또 사기** — 내 번호의 추첨 전 기록에서 번호를 클립보드에 복사하고 동행복권
  구매 화면을 새 탭으로 연다. (동행복권은 번호를 주소로 받는 공식 방법이 없어 화면만 열어 준다.)
- **QR 파서** (`lottoracle/qr.py`, `POST /api/qr`) — 용지 QR 의 v 값을 회차·조합으로 파싱해 바로 채점.
  앱 연동을 염두에 둔 백엔드이고, 웹 GUI 는 번호 직접 입력을 쓴다.
- 당첨 확인 탭에서 **내 번호 가져오기** 로 저장한 조합을 그대로 불러올 수 있다.

### 변경
- '열두 띠의 오늘' → **'띠로만 보는 오늘'**. 개인 운세와 계산이 다르다는 설명을 붙이고,
  프로필이 있으면 내 띠는 표에서 빼서 두 결과가 어긋나 보이지 않게 했다.
- 탭 이름 '채점' → '당첨 확인'.

## 0.2.0 — 2026-09-03

### 추가
- **오늘의 운세** 탭이 첫 화면. 이름·생년월일·태어난 시(선택)를 한 번 받아 프로필로 저장하고,
  날짜+프로필로 결정되는 운세(등급·문장·오늘의 숫자·색·한 단어)를 보여준다.
  "이 기운으로 번호 뽑기"를 누르면 추천 입력(생일·띠·행운수)이 자동으로 채워진다.
- 열두 띠의 오늘 (프로필 없이도 표시).
- 생년월일 입력은 년·월·일 세 칸으로, 년 4자리 → 월 2자리 → 일 자동 이동. 태어난 시는
  십이지 시간대 선택(30분 보정: 자시 23:30~01:30 … 진시 07:30~09:30)으로 표시.
- **내 번호** 탭: 추천 결과를 목표 회차와 함께 저장하고, 추첨 후 자동 채점.
- 시작할 때 새 회차 자동 확인 + 회차 탭의 "최신 회차 받기" 버튼.
- **복권방** 탭: 카카오맵 지도·검색. JavaScript 키가 내장되어 localhost:8765 에서 바로 동작하고,
  다른 주소로 열면 카카오맵·네이버 지도 링크로 대체된다.
- **설정 · 정보** 탭: 자동 갱신, 프로필 관리, 명시적 면책 문구.
- CLI `fortune`, `profile show|set|clear`, `recommend --profile`.
- GitHub Actions CI (Python 3.10~3.13).

### 변경
- 동행복권 API 변경(`lt645/selectPstLt645Info.do`)에 맞춰 `fetch` 수정. 최신 회차를 API로 확인해 필요한 만큼만 받는다.
- 화면 상단의 큰 경고 상자를 걷어내고, 추천 결과 아래 한 줄과 설정·정보 탭의 정확한 문구로 나눴다.
- 쓰기 API 에 Origin 검사(CSRF 방지). `--host` 를 루프백이 아닌 주소로 열면 콘솔에 경고.
- 포트 충돌 시 친절한 오류 메시지.

### 개인정보
- 프로필·내 번호·설정은 `data/profile.json`, `data/picks.json`, `data/settings.json` 에만 저장되며
  외부로 전송하지 않는다. 셋 다 `.gitignore` 에 들어 있다.

## 0.1.0

- 첫 공개. 통계 필터 + 민간속설 가중치 추천, 브라우저 GUI, 채점, 백테스트, 1~1239회차 실데이터.
