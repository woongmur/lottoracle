# Changelog

## 0.2.0 — 2026-09-03

### 추가
- **오늘의 운세** 탭이 첫 화면. 이름·생년월일·태어난 시(선택)를 한 번 받아 프로필로 저장하고,
  날짜+프로필로 결정되는 운세(등급·문장·오늘의 숫자·색·한 단어)를 보여준다.
  "이 기운으로 번호 뽑기"를 누르면 추천 입력(생일·띠·행운수)이 자동으로 채워진다.
- 열두 띠의 오늘 (프로필 없이도 표시).
- 생년월일 입력은 년·월·일 세 칸으로, 년 4자리 → 월 2자리 → 일 자동 이동. 태어난 시는
  십이지 시간대 선택(30분 보정: 자시 23:30~01:30 … 진시 07:30~09:30)으로 표시.
- **내 번호** 탭: 추천 결과를 목표 회차와 함께 저장하고, 추첨 후 자동 채점.
- 시작할 때 새 회차 자동 확인 + 회차 탭의 "최신 회차 받기" 버튼.
- **복권방** 탭: 카카오맵 지도·검색. 기본 JavaScript 키가 내장되어 localhost:8765 에서 바로 동작하고,
  다른 주소로 열 때는 설정 탭 또는 `LOTTORACLE_KAKAO_JS_KEY` 로 자기 키를 넣는다.
- **설정 · 정보** 탭: 카카오맵 키, 자동 갱신, 프로필 관리, 명시적 면책 문구.
- CLI `fortune`, `profile show|set|clear`, `recommend --profile`.
- GitHub Actions CI (Python 3.10~3.13).

### 변경
- 동행복권 API 변경(`lt645/selectPstLt645Info.do`)에 맞춰 `fetch` 수정. 최신 회차를 API로 확인해 필요한 만큼만 받는다.
- 화면 상단의 큰 경고 상자를 걷어내고, 추천 결과 아래 한 줄과 설정·정보 탭의 정확한 문구로 나눴다.
- 쓰기 API 에 Origin 검사(CSRF 방지). `--host` 를 루프백이 아닌 주소로 열면 콘솔에 경고.
- 포트 충돌 시 친절한 오류 메시지.

### 개인정보
- 프로필·내 번호·설정은 `data/profile.json`, `data/picks.json`, `data/settings.json` 에만 저장되며
  외부로 전송하지 않는다. 셋 다 `.gitignore` 에 들어 있다.

## 0.1.0

- 첫 공개. 통계 필터 + 민간속설 가중치 추천, 브라우저 GUI, 채점, 백테스트, 1~1239회차 실데이터.
